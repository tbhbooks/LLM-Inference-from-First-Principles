# Claude Instructions

Read `AGENTS.md` first. It is the source of truth for this TBH book.

If the Claude TBH plugin is installed, `/tbh:*` commands should follow the same behavior described by `AGENTS.md` and `tbhbooks-agent-kit`.
